# LEARNINGS-120710421

Checkpoint 1:
`git add tests/test_calculator.py`
`git commit -m "fix"`
`git add src/calculator.py`
`git commit -m "update"`
`git push origin feature/120710421`

Reflection:
I learned how to use the staging area to separate unrelated changes into different commits. This is important because it keeps history cleaner and makes each commit easier to understand.

Checkpoint 2:
`git log --oneline`
`git revert --no-edit 66f6716`

Reflection:
I learned how to use `git log` to identify commits and `git revert` to undo commits without hiding history. This is helpful because it ensures there is a record for previous actions with the vcs.

Checkpoint 3:
`git switch main`
`git add src/validator.py`
`git commit -m "Add is_positive helper function"`
`git reset --hard HEAD~1`
`git switch feature/120710421`
`git cherry-pick 91d4935920aa244f2d4610355bdc514a12cd6d84`
`git add src/validator.py`
`git cherry-pick --continue`
`git switch main`
`git log --oneline`

Reflection:
I learned how to move commits from the wrong branch to the correct one using `git cherry-pick`. This is useful if I were to not pay attention to the branch I was on in the future.

Checkpoint 4:
`git switch -c experiment/120710421`
`git add src/validator.py`
`git commit -m "add comment for checkpoint 4 to validator.py"`
`git switch feature/120710421`
`git branch -D experiment/120710421`
`git reflog`
`git branch recovered/120710421 6af25c6`
`git merge --no-ff recovered/120710421 -m "Merge recovered branch for checkpoint 4"`

Reflection:
I learned how to use `git reflog` to recover work after deleting a branch. This is useful because I can now recover any commits lost and merge them appropriately.

Checkpoint 5:
`git remote add upstream https://github.com/mianaomi/git_project_1.git`
`git fetch upstream`
`git switch main`
`git merge --ff-only upstream/main`
`git switch feature/120710421`
`git rebase --rebase-merges main`
`git push --force-with-lease origin feature/120710421`

Reflection:
I learned how to work with a remote repo and rebase my feature branch onto an updated main branch and why `--force-with-lease` is used. This is useful because I can contribute to repositories that are not local to my device.

Checkpoint 6:
`git log main..HEAD --oneline`
`git rebase -i --rebase-merges main`
`git commit --amend -m "Fix calculator tests to import modulo"`
`git commit --amend -m "Add calculator module docstring"`
`git rebase --continue`
`git push --force-with-lease origin feature/120710421`

Reflection:
I learned how to clean commit history with interactive rebase and improve commit messages so the branch has more appropriate commits. This is useful because git history should tell a story, making it easy to review git history.
