Each checkpoint entry (CP1–CP6) is worth 10 points:



Commands (5 pts): List the exact git commands you ran for that checkpoint, in order.

Reflection (5 pts): 1–2 sentences explaining what you learned and why it matters.



1

pytest tests/

git commit -m "fix"

git commit -m "update"

git push origin feature/118735030

git push --force origin feature/118735030 

Learned to stage and separate changes into different commits. Good for being able to target rollback.

2

git log --oneline

git revert b19f233 Revert "BAD: Add excessive debug logging to calculator module"

Learned to undo commits, a very important part of VC as a system.

3

git checkout main

git add src/validator.py

git commit -m "Add is\_positive helper function"

git log --oneline

git reset --hard HEAD~1

git log --oneline

git checkout feature/118735030

git cherry-pick 07b57cd

git add src/validator.py

git cherry-pick --continue

git push origin feature/118735030

Learned to move commits from branch to branch, keeps maintained versions clean.

4

git checkout -b experiment/118735030

git add .

git commit -m "c4 change"  

git checkout feature/118735030    

git branch -D experiment/118735030

git reflog

git checkout -b recovered/118735030 9d2c29d

git checkout feature/118735030

git merge recovered/118735030

git push origin feature/118735030

I learned that you can delete commits and recover them.

5

git remote add upstream https://github.com/mdurrani808/git\_project\_1.git

git remote -v

git fetch upstream

git reset --hard upstream/main

git checkout feature/118735030

Well for one, I learned what upstream (the root of your own branched fork) is, and the other, that/how you can pull any changes down the line to your versions.

6

git log main..HEAD --oneline

git rebase -i main

git push --force origin feature/118735030

git log --oneline

This one was informative. I didn't know you even could rename a commit, so cleaning up the history was interesting.







